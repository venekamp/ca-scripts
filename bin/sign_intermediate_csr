#!/bin/sh

DIR=$(pwd)
CONFIG_DIR="$DIR/config"
source $CONFIG_DIR/globals

D1=$(date '+%s')
D2=$(date -jnu -f '%s' -v+$INTERMEDIATE_CA_VALIDITY "$D1" '+%s')

DAYS=$(get_date_difference_in_days "$D1" "$D2")

sign_csr $INTERMEDIATE_CA_CSR $INTERMEDIATE_CA_CERT $DAYS


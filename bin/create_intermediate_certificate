#!/bin/sh

DIR=$(pwd)
CONFIG_DIR="$DIR/config"
source $CONFIG_DIR/globals

create_certificate $INTERMEDIATE_CA_CERT $INTERMEDIATE_CA_KEY $ROOT_CA_OPENSSL_CONFIG $INTERMEDIATE_CA_VALIDITY $INTERMEDIATE_CA_OPENSSL_EXTENSION $INTERMEDIATE_CA_CSR
